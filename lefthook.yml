pre-commit:
  parallel: true
  commands:
    biome-check:
      glob: "*.{js,ts,tsx,json}"
      run: bunx biome check --write --no-errors-on-unmatched --staged {staged_files}
      stage_fixed: true

commit-msg:
  commands:
    commitlint:
      run: bunx commitlint --edit {1}
