pre-commit:
  parallel: true
  commands:
    check:
      glob: "*.{js,ts,tsx,json}"
      run: vp check --fix {staged_files}
      stage_fixed: true

commit-msg:
  commands:
    commitlint:
      run: pnpm exec commitlint --edit {1}
