---
"@fideus-labs/fidnii": minor
---

Re-export `ngffZarrConfig` and `setWorkerPoolSize` from `@fideus-labs/ngff-zarr/browser`, allowing consumers to configure the number of Web Workers used by ngff-zarr's codec, write, and omero pools. Added a Worker Pool Size slider (1–16) to the dev/test page UI.
